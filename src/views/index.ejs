<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <a href="/create">Add new message</a>
    <ul>
      <% messages.map((message, index) => { %>
      <li>
        <h1><%= message.user %></h1>
        <h3><%= message.content %></h3>
        <a href="/<%= index %>">More Details</a>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
